lista = [i for i in range(1,10)] #otra forma de inicializar con todos los numeros dentro de un rango

print(f"primer elemento = {lista[0]}")
print(f"elemento medio = {lista[int(len(lista)/2)]}")
print(f"ultimo elemento = {lista[len(lista)-1]}")